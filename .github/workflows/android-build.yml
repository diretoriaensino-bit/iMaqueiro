name: Build iMaqueiro APK

on: 
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          
      - name: Install Capacitor
        run: |
          npm install @capacitor/core @capacitor/cli @capacitor/android @capacitor/push-notifications
          
          # A MÁGICA DE LIMPEZA AQUI: Apaga qualquer pasta "android" ou configuração antiga que tenha subido por engano
          rm -rf android capacitor.config.json capacitor.config.ts
          
          # Começa a construção limpa
          npx cap init "iMaqueiro Pro" br.com.imaqueiro.pro --web-dir .
          npx cap add android
          npx cap sync
          
      - name: Build Android APK
        run: |
          cd android && ./gradlew assembleDebug
          
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: imaqueiro-pro-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
